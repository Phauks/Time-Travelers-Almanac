// Fully static site for GitHub Pages: prerender everything, and emit a
// directory/index.html per route so the host serves clean URLs.
export const prerender = true;
export const trailingSlash = 'always';
